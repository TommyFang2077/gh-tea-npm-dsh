# dsh bundle layer: mount the gh-tea-npm plugin (dsh/index.js via the package
# root export). Host-only plugin: registers the gh/tea/npm forge tools and the
# companion gh-tea-npm skill.
- insert:
    - id: gh-tea-npm
      name: '@tommyfang/gh-tea-npm-dsh'
